#ifndef DETERMINISTIC_REGEX_PLUGIN_H
#define DETERMINISTIC_REGEX_PLUGIN_H

#include "deterministic_draft_plugin.h"

// Regex filter plugin: allows only tokens made of letters and whitespace
const char * deterministic_regex_plugin_get_version(void);

#endif
